#include <vector>
#include <string>

class Solution {
public:
    std::string encode(std::vector<std::string>& strs) {
        return "";
    }

    std::vector<std::string> decode(std::string s) {
        return {};
    }
};
